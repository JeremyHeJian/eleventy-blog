Write comprehensive unit tests for the Python code in: **$ARGUMENTS**

**Testing guidelines:**

- Use the pytest framework for writing test functions.
- Create a new test file named `<module>_test.py` in the `tests/` directory (where `<module>` comes from $ARGUMENTS).
- Cover typical use cases, edge cases, and error conditions for all functions.
- Include assertions for expected outputs and exceptions.
- Ensure the tests are self-contained and adhere to PEP 8 style conventions.

After writing, print: Finished generating tests for $ARGUMENTS
